<h1>Full Menu</h1>
 <%= link_to 'New order', new_order_path %>

<table>
	<tr>
		<th>Item</th>
		<th>Price</th>
		<th colspan="5"></th>
	</tr>
	<% binding.pry%>
	<% @menu.each do |item| %>
		<tr>
			<td><%= item.item %></td>
			<td><%= item.price %></td>
		</tr>
	<% end %>
</table>